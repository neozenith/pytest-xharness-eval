# Authored record (ADR 0048). The .md beside this file is GENERATED:
#   make adrs
# Edit this file, never the markdown.
id: ADR-0024
slug: context-window-metrics-injected-messages-and-design-tokens
plan_id: null
title: Context window consumption and timing are first-class metrics; injected messages are not prompts;
  the report is themed by design tokens and can be written standalone
description: report what the provider measured, and qualify every percentage by its denominator
status: accepted
accepted_on: '2026-08-23'
last_changed_on: '2026-08-23'
group: accounting
tags:
- accounting
- report
lens: |-
  Report what the provider measured, qualify every percentage by its denominator, and let the owner of the report own its palette.
provenance: |-
  Both harnesses already report the context window (Claude: `modelUsage[<model>].contextWindow`; Codex: `model_context_window`) and time their runs, and neither figure reached the report.
enforced_in:
- src/pytest_xharness_eval/model/runresult.py
- src/pytest_xharness_eval/harness/records.py
- src/pytest_xharness_eval/assets/report.tokens.json
- src/pytest_xharness_eval/emit/tokens.py
- docs/token-accounting.md
relates_to:
- relation: extends
  target: ADR-0019
  note: the record's status line says "refines"
- relation: extends
  target: ADR-0020
  note: the record's status line says "refines"
- relation: extends
  target: ADR-0022
  note: the record's status line says "refines"
problem:
  symptom: |-
    Three gaps in the report after 0023.

    Nothing said how close a run came to its model's context window.
    Both harnesses report the window: Claude as `modelUsage[<model>].contextWindow`, Codex as `model_context_window` on `task_started` and every `token_count`.
    Both also time their runs: Claude with `ttft_ms` and `duration_api_ms`, Codex with `time_to_first_token_ms` and `duration_ms`.

    Codex writes its environment and plugin notes as *user*-role messages, such as `<environment_context>` and `<recommended_plugins>`.
    It writes its policies as developer messages, such as `<permissions>` and `<skills_instructions>`.
    The catalogue of 0022 coloured the user-role ones as the prompt under test; they are harness context.

    The page's colours lived in its CSS.
  pain_point: |-
    Thinking complicates the question of "how full is the context".
    The provider withholds thinking text server-side, and may drop earlier turns' thinking from later prompts.
    A reader therefore cannot add up what they see.
    Harness context read as the prompt under test.
    And there was no way to brand a report, and no way to hand one file to someone without a web server.
decision:
  given: |-
    Both harnesses already measure the window and the timings, so nothing has to be estimated.
  we_prefer: |-
    Reporting the provider's own measured figures, over layering an estimate on top of what a reader can see.
    A token file the project owns, over colours embedded in the page's CSS.
  because: |-
    Whatever the server retained or dropped of earlier thinking is already inside the provider's count.
    The owner of the report should own its palette.
  unless: |-
    the harness reports no window, in which case percentages are `null`, never guessed.
  in_practice:
  - |-
    **Context window.** `RunResult.context_window` is the window the harness reported for the model.
    Per turn, `context_tokens` (`input + cache_read + cache_write`) is the prompt the provider says it processed, and `context_pct` is that over the window.
    These are *measured*.
    Whatever the server retained or dropped of earlier thinking is already inside the provider's count, so no estimate is layered on top.
    `context_window_pct` (peak turn) and `final_context_pct` (last prompt plus its output) are top-level metrics, always qualified by the window size.
    They appear on the history line, the status word (`ctx 7.2%`), the session table and `SessionMetaTable`.
    A line of the log is annotated with the measured figure of the turn it belongs to.
    A tool result is annotated with the next turn's figure, because that is the prompt it became part of.
    When the harness reports no window, percentages are `null`, never guessed.
  - |-
    **Timing.** `ttft_ms` and `api_duration_ms` come from the harness; `output_tokens_per_sec` is output tokens over API time (else agent time).
    Per turn, `latency_ms` is the wall time from the previous log record to the turn's first record (request plus generation) and `output_tokens_per_sec` follows.
  - |-
    **Injected messages.** A user-role message whose text opens with an XML-style tag classifies as `.../injected` (category `harness_context`): `codex/response_item/message/user/injected`, `codex/event_msg/item_completed/UserMessage/injected`, `claude/user/injected`.
    The report renders every tagged message, whatever its role, as titled blocks per top-level element rather than as prose.
  - |-
    **Design tokens.** The page's palette lives in `report.tokens.json`.
    It covers per-theme colours, chart series, waterfall colours, category pill colours, fonts and the highlight style.
    The file is bundled with the package and copied beside every report, so it can be edited in place and refreshed.
    A project points at its own file with the ini key `xharness_report_design_tokens` or the flag `--xharness-report-design-tokens FILE`.
    The replay command takes `--design-tokens`.
    A missing or malformed file is an error, not a fallback.
    The CSS keeps a fallback palette only for the moment before the tokens load.
  - |-
    **Standalone report.** `xharness_report_inline` / `--xharness-report-inline` (replay: `--inline`) writes `report.html` with everything embedded on `window.__XH_DATA__`.
    That is the index, every result, every session log and the tokens.
    The one file therefore opens over `file://`, and can be attached or mailed.
    `</` inside embedded data is written `<\/` so a log cannot terminate the script.
consequences:
  pros:
  - |-
    Every percentage is qualified by its denominator, and a run that reports no window reports `null` rather than a guess.
  cons:
  - |-
    Results and history written before this decision have no window or timing fields; the replay command fills them from the captured logs and envelopes.
  - |-
    Inline reports are large (they carry every log) and go stale the moment a cell re-runs; the fetching form stays the default.
  - |-
    Per-turn latency on Claude includes the harness's own time between receiving a tool result and sending the next request.
    It is an upper bound on model time, which is why the session-level figure uses the harness's `duration_api_ms` where available.
