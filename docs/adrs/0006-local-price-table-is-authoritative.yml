# Authored record (ADR 0048). The .md beside this file is GENERATED:
#   make adrs
# Edit this file, never the markdown.
id: ADR-0006
slug: local-price-table-is-authoritative
plan_id: null
title: A local prices.toml is authoritative, with an optional upstream seed
description: an upstream feed fills the table but is never the thing the harness reads
status: accepted
accepted_on: '2026-08-21'
last_changed_on: '2026-08-21'
group: pricing
tags:
- pricing
lens: |-
  Keep the price table owned, not generated.
  An upstream is a convenience for filling it, never the thing the harness reads.
provenance: |-
  Neither CLI's session log carries cost, and Codex reports none anywhere; the LiteLLM feed was fetched and checked against both providers' model names.
enforced_in:
- src/pytest_xharness_eval/derive/prices.toml
- src/pytest_xharness_eval/derive/pricing.py
relates_to:
- relation: extended_by
  target: ADR-0014
  note: >-
    the table is now bundled inside the package and layered with the consumer's
    optional `xharness_prices` file
- relation: superseded_by
  target: ADR-0030
  note: >-
    the project-level override moved from a rootdir `prices.toml` into
    `xharness_prices` ini lines; the bundled table is unchanged
problem:
  symptom: |-
    Neither CLI's session log carries cost, and Codex reports none anywhere.
  pain_point: |-
    The LiteLLM feed carries per-token input, output, cache-read, and cache-write rates for Anthropic models.
    Its coverage of Codex's aliased model names (for example `gpt-5.6-sol`) is unconfirmed, so a generated table would silently lose rows.
decision:
  given: |-
    An upstream feed's coverage of one provider's aliased names cannot be relied on.
  we_prefer: |-
    A committed `prices.toml` as the single authority for rates, over reading an upstream feed directly at run time.
  because: |-
    A table the repository owns cannot lose a row to an upstream rename, and the upstream stays useful for filling it.
  unless: never
  in_practice:
  - |-
    A `--seed-prices` command will populate and refresh rows from upstream.
    It never overwrites a row marked as a local override, and it reports every model it could not match.
consequences:
  pros:
  - Claude rates can be refreshed from a real upstream.
  - Relative comparison across cells stays robust regardless of absolute rate drift.
  cons:
  - Codex aliases stay hand-entered.
  - |-
    Until the seed command exists, every row is maintained by hand, and absolute USD figures lag provider changes.
