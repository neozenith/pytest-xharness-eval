# Authored record (ADR 0048). The .md beside this file is GENERATED:
#   make adrs
# Edit this file, never the markdown.
id: ADR-0044
slug: a-skill-is-invoked-the-way-its-user-invokes-it
plan_id: null
title: A skill is invoked the way its user invokes it, in each harness's own syntax
description: an eval that has to explain the harness to the agent is measuring the harness
status: accepted
accepted_on: '2026-08-31'
last_changed_on: '2026-08-31'
group: harness
tags:
- harness
- collection
lens: |-
  An eval that has to explain the harness to the agent is measuring the harness.
  The prompt is not a place to reconcile two loading mechanisms.
  The reconciliation belongs behind the interface that already exists to hold the difference.
  What the case declares is the part a user would actually say.
provenance: |-
  Both CLIs have a documented invocation, and they are not the same.
  `/<skill> <task>` was verified on claude 2.1.251 against `--setting-sources ""`.
  `$<skill> <task>` was verified on codex-cli 0.151.0 against `--ignore-user-config`.
enforced_in:
- src/pytest_xharness_eval/harness/base.py (`invoke`, and the `evals/` exclusion)
- src/pytest_xharness_eval/harness/claude.py (the `--plugin-dir` wrapper)
- src/pytest_xharness_eval/harness/codex.py
- src/pytest_xharness_eval/model/case.py (`task=`, and the `prompt=` TypeError)
relates_to:
- relation: extends
  target: ADR-0011
  note: the record's status line says "refines"
- relation: extends
  target: ADR-0034
  note: the record's status line says "refines"
- relation: supersedes
  target: ADR-0008
  note: supersedes the `prompt=` field of `@evalcase` established there
problem:
  symptom: |-
    0011 settled that a skill reaches the agent through each CLI's own loading path.
    The two adapters implemented it: Claude got `--add-dir <skill dir>`, and Codex a copy into the private `CODEX_HOME/skills/`.
    Two different mechanisms, as 0011 intended.

    But the *prompt* stayed one string, shared by every cell of the case.
    It had to paper over the difference the adapters had just introduced.
    Every case in every consuming repository opened the same way:

    > Use the mermaidjs-diagrams skill (its SKILL.md is in the extra allowed directory / your skills).

    That sentence is the tell.
    "The extra allowed directory / your skills" is a prompt written to survive two loading paths at once, and it names neither mechanism correctly:

    | What the prompt asked for | What actually happens |
    | --- | --- |
    | Claude: "its SKILL.md is in the extra allowed directory" | `--add-dir` grants *tool access* to a path. It does not register a skill. The agent has to be told a path, then read it as a file, then decide to follow it: three steps a real user never performs. |
    | Codex: "/ your skills" | The skill *is* registered, and Codex's own instructions say the trigger is `$SkillName`. The prompt never used it, so the skill was reached by description-match at best. |
  pain_point: |-
    So the matrix's two arms were not running the same experiment.
    One arm graded a skill loaded as a document; the other graded a skill loaded as a skill.
    Neither arm graded what a user does, which is the only invocation whose failure anybody would care about.
    A skill whose `SKILL.md` frontmatter is malformed passed both arms, because being told to read a file does not require the file to be registered.
    That is the single most common way a skill breaks for its users.
decision:
  given: |-
    Both CLIs have a documented, verified invocation, and they are not the same:

    | Harness | Registration | Invocation a user types | Verified |
    | --- | --- | --- | --- |
    | `claude` | `--plugin-dir <dir>`, where the dir carries `.claude-plugin/plugin.json` and `skills/<skill>/` | `/<skill> <task>` | claude 2.1.251, against `--setting-sources ""` |
    | `codex` | a copy under the private `CODEX_HOME/skills/<skill>/` | `$<skill> <task>` | codex-cli 0.151.0, against `--ignore-user-config` |

    Claude's `--add-dir` was never a registration; `--plugin-dir` is.
    It is the only one that keeps 0005's isolation intact.
    A private `CLAUDE_CONFIG_DIR` would take the credentials with it, and `--setting-sources user` needs them.
  we_prefer: |-
    A `task=` the case declares, plus a `Harness.invoke` that renders each CLI's own syntax.
    That is preferred over one shared `prompt=` string that names a loading path.
  because: |-
    An eval that has to explain the harness to the agent is measuring the harness.
    The two arms were not running the same experiment.
  unless: never
  in_practice:
  - |-
    **A case declares a task, not a prompt**.
    `@evalcase(task=...)` is the sentence a user would type *after* naming the skill.
    An example is "Apply the palette mandate to ARCHITECTURE.md".
    It never names the skill, never names a loading path, and never mentions a CLI.
  - |-
    **The harness renders the invocation.** `Harness.invoke(skill=, task=)` is a new abstract member beside the four 0034 already requires.
    It is the only place either syntax is spelled: `claude` renders `/<skill> <task>`, and `codex` renders `$<skill> <task>`.
    Nothing outside `harness/` composes an invocation, so a third CLI with a third syntax is still the one-file job 0034 promises.
    The one lookup from underneath goes through `model/registry.py`, 0039's declared edge.
  - |-
    **Registration is the adapter's job, and it is real registration.** The Claude adapter builds a wrapper plugin beside the workspace, and passes `--plugin-dir`.
    That plugin is a `.claude-plugin/plugin.json` plus a copy of the skill under `skills/`.
    A skill whose frontmatter Claude rejects now fails the cell, which is the point.
  - |-
    **The rendered prompt is evidence.** `CaseRef` carries both: `task`, the case's declaration, and `prompt`, the exact string that reached the CLI.
    A reader of one `result.json` can see what was asked without holding the suite file.
    Two cells of one case can be compared knowing precisely how their asks differed.
consequences:
  pros:
  - |-
    A cell can now fail for a reason it previously could not: the skill did not resolve.
    That is a true negative.
    `/nosuchskill` comes back as "Unknown command" with zero billed tokens, so the evidence gate of [0045](0045-the-verifiers-ship-with-the-plugin.md) catches it before any grader reads a workspace.
  - |-
    **The skill's own `evals/` tree was being handed to the agent under test**.
    Both loading paths copy the skill directory, and since 0046 that directory contains the goldens.
    The exclusion is now in the shared spawn contract (`harness/base.py`), not in either adapter, so a third provider cannot reintroduce it.
  cons:
  - |-
    Every existing suite is a breaking change, and deliberately so.
    A `prompt=` that still carries "use the X skill (its SKILL.md is in ...)" is exactly the text this record exists to delete.
    `@evalcase` raises on `prompt=` with a message naming `task=`, rather than accepting it as an alias.
    An alias would let the old sentence survive the upgrade.
  - |-
    Captured runs from before this record carry `case.prompt` and no `case.task`.
    A replay reads the task back as empty rather than re-deriving it, per [0038](0038-a-tolerant-reader-still-honours-its-declaration.md).
  - |-
    **The dialects disagree about whether `SKILL.md` is ever *read*.** Same task, same fixture.
    `claude` resolves `/<skill>` by injecting the file, so it never appears in skill coverage.
    `codex` is instructed to read it, and so it does.
    An eval that asserts `SKILL.md` was loaded therefore passes on one arm and fails on the other.
    The reason is not about the skill.
    That is precisely the failure this record exists to end, reappearing one level down.
    `check_skill_was_loaded` has no default, and refuses an empty call.
    What a case names is the `resources/` and `scripts/` material the entry point points at, which both dialects reach when they follow it.
