# Design campaign log — AAA component refinement

Living record of the Opus designer / adversarial-council campaign over every
component unit, leaf → App shell (workflow `aaa-design-campaign`, run `wf_d8b9902e-50c`).
Regenerated from the campaign journal; the *Open residuals* section is the checklist
for the post-campaign cleanup pass.

## Unit checklist

- [x] **ui-primitives** — Primitive kit: button, badge, card, switch, collapsible, toggle-group, tooltip, table + base/table/chip CSS
  - status: REJECTED at score 7.5 after 3 round(s)
- [x] **chart-frames** — Chart chrome: ChartPanel, Plot + right-hand legend, Plotly base layout, ChartAxisToggle
  - status: REJECTED at score 6.5 after 3 round(s)
- [~] **records** — Session-log record cards: .rec header rhythm, code blocks, kv grids, blocks, collapsibles
  - status: round 3 in review
- [ ] **panels** — Analytical panels: KvTable/Chip/Pill, Reconciliation, CostByTier, RecordKinds, SkillCoverage, SessionTurnTable
  - status: queued
- [ ] **session-table** — Overview sessions table: sortable headers, verdict badges, CopyId, El
  - status: queued
- [ ] **nav-sidebar** — Collapsible 5-level nav tree + section jump-links
  - status: queued
- [ ] **report-header** — Sticky header: breadcrumb title, meta line, theme toggle
  - status: queued
- [ ] **views** — Page composition: SweepOverview, SessionView grid + rhythm, App container spacing
  - status: queued
- [ ] **app-shell** — Whole-app coherence pass: background/surface contrast, focus states, loading, inconsistencies
  - status: queued

## Open residuals (cleanup-pass checklist)

### ui-primitives
- [ ] `src/components/ui/collapsible.tsx` — the Trigger has NO hover state. Verified by byte-comparing element screenshots of `.env-trigger` before and after `hover()`: identical (`hover changes pixels: false`), on a control measuring 1008 x 40px. Add `hoverStyle` and `pressStyle` to `TamaguiCollapsible.Trigger` inside `CollapsibleTrigger` (props, not stylesheet — Tamagui's `:root:root ._bg-…hover:hover{…!important}` atomics outrank any plain class rule, per the note at index.css:776). Concretely: `hoverStyle={{ backgroundColor: "$code", color: "$color" }}`, `pressStyle={{ backgroundColor: "$code",  …
- [ ] `src/components/ui/collapsible.tsx` — the Trigger inherits Tamagui's `_fd-column` / `_ai-center` frame defaults, so the disclosure renders as a full-card-width, centre-aligned two-line stack (chevron on its own line ABOVE the label). Measured computed style: `display:flex, flex-direction:column, align-items:center, text-align:center, width:1008.03px`. This directly contradicts `.env-trigger` in index.css (`display:flex; align-items:center; gap:0.25rem`), which is silently losing. Fix on the Tamagui side in `CollapsibleTrigger`: pass `flexDirection="row"`, `alignItems="center"`, `justifyContent …
- [ ] `src/components/ui/collapsible.tsx` — after the two fixes above, confirm the trigger still clears the kit's 28px hit-target floor once it stops spanning the card (it is currently 40px tall only because of the stacked layout). Give it an explicit `minHeight={28}` on `TamaguiCollapsible.Trigger` so the shrink-to-content row layout cannot drop it below the floor that `Button size="sm"`, `.filter-chip` and the toggle-group segments all hold to.

### chart-frames
- [ ] Ragged plot right edge across the four stacked session charts. Measured live at 1440px in BOTH themes (`.ygrid` bounding rect): TokenWaterfallChart right=1131, ContextWindowChart right=1387, OutputPerTurnChart right=1131, TurnTiersChart right=1131 — a 256px jump on the second chart in a single vertical column of four (left edges all agree at 365). The frame causes it: `ChartLegend` returns `null` when `items` is empty (Plot.tsx:106), so a legendless chart collapses the 240px column + 16px `columnGap` its siblings reserve, and ContextWindowChart therefore uses a bare `<Plot>` and runs full-blee …
- [ ] The Plotly hover box is not a member of the page's floating-surface family. `baseLayout.hoverlabel` (src/components/charts/plotly.ts:162-173) sets `bgcolor: t.panel` — byte-identical to the card it floats over — with `bordercolor: t.muted` and no elevation. Measured live on the overview chart: light `fill rgb(255,255,255)` / `stroke rgb(91,96,112)` 1px, dark `fill rgb(23,26,35)` / `stroke rgb(154,160,176)` 1px. That stroke is ~7:1 against its own ground, the loudest border anywhere on the page (every other border is `--xh-line`, ~1.2:1), and it is the ONLY thing separating the box from the car …

## Round-by-round record

### ui-primitives
#### Round 1
**Designer changes:** Changed, and why each one is better: **`ui/tooltip.tsx` — fixed a real defect.** Every tooltip on the page was rendering roughly one word per line: the popper shrink-wrapped to its narrowest box and `maxWidth={320}` never engaged. Added `width="max-content"` with `maxWidth={280}`, plus 10/6 padding. Tooltips now read as single lines of prose in both themes. **`ui/button.tsx` — tactility and target size.** The outline variant gains a resting `0 1px 2px -1px` elevation that strengthens on hover and *disappears* on press — the shadow leaving under the 0.97 scale is what sells the travel; before, press was scale-only. `icon-sm` 30→32 so the theme and nav toggles match the default button height and clear the hit-target floor. Radius 7→8, aligning buttons with `.filter-chip`, `.rec` and `.nav-link` on one radius scale (6 xs / 8 control / 12 card / 999 pill). **`ui/toggle-group.tsx` — concentric and reachable.** Item 26→28px (hit target), radius 6→7 so the thumb is concentric inside the track's 9px with its 2px inset, instead of looking rounder than the slot it sits in. **`ui/switch.tsx` — the off state was ambiguous.** A full-strength `$muted` thumb on a flat `$code` track read as a *filled* control, i.e. "on". The track is now a shade deeper than the panel (a recessed slot) and the knob is muted pulled 28% toward the panel — reads unmistakably as off, in both themes. **`ui/badge.tsx …

**Council:** REJECTED (score 6.5)
- ACCESSIBILITY — the sortable table-header focus ring is clipped on every column. `.table-scroll` sets `overflow-x: auto`, which forces the computed `overflow-y` to `auto` (verified in the DOM: `overflowY: "auto"`, clientWidth 1086 / scrollWidth 2088). The global `:where(a[href], button, …):focus-visible { outline: 2px solid var(--xh-accent); outline-offset: 2px }` ring on `.xh-table thead th button` therefore renders as an open-topped "U" — the top edge is cut flat on EVERY header, and the left edge is also missing on the first column (`padding-left: 0; margin-left: 0` puts the button flush wi …
- CSS SPECIFICITY BUG — a selected filter chip loses its accent border on hover. `button.filter-chip:hover { border-color: color-mix(in srgb, var(--xh-muted) 55%, var(--xh-line)) }` has specificity (0,2,1) and beats `.filter-chip[data-on="true"] { border-color: var(--xh-accent) }` at (0,2,0). So hovering the ON chip turns its border grey while `button.filter-chip[data-on="true"]:hover` simultaneously pushes the background to a STRONGER accent 20% — the selected state visibly degrades under the pointer. Confirmed in both themes on the SkillCoveragePanel `files 18` chip. Fix: add `border-color: va …
- DATA INTEGRITY — the new scroll wash signals the clip but does not stop content being guillotined mid-glyph, which was the stated goal. At rest the right edge of SessionTable shows `$0.069` where the value is `$0.0695`, and a header reading `estimated_cost_us`; scrolled fully right the left edge shows `timated_cost_usd`. A right-aligned currency column cut to a shorter, still-plausible number is a misread, not just a rough edge — the wash paints on the container's background layer, behind the cells, so it never touches the glyphs. Fix in `.table-scroll`: apply the same two-stop edge gradients  …
- HARD-CONSTRAINT VIOLATION — hardcoded hex in the chip section of src/index.css: `.pill { … color: #fff; }`. Colours must come from a token. Add `--xh-on-category: #ffffff;` to BOTH `:root` and `.dark` (the value is deliberately identical in both themes because the pill's background is a fixed per-category colour, not a theme surface — say so in a one-line comment) and change `.pill` to `color: var(--xh-on-category);`.

_Notes:_ METHOD: gates run by me — `bun run check` (tsc + eslint + prettier) and `bun run test` (8 files, 61 tests) both PASS. Took my own screenshots at 1440x900 and 900x900, deviceScaleFactor 1–4, light and dark, for: overview, session view, detailed records drill-down, ReportHeader/ThemeToggle, NavToggle, the ghost back button, the xs outline `raw/nice` flip in RecordCard, the toggle group, the switch, VerdictBadge, filter chips, table rows (rest/hover/open), sortable header focus, tooltip, card-vs-pa …

#### Round 2
**Designer changes:** All four mandatory fixes landed, plus two related defects the same audit turned up. `bun run format && bun run check && bun run test` all pass (61 tests, 8 files). **`src/index.css`** 1. **Focus ring un-clipped (council #1).** Added `.xh-table thead th button:focus-visible { outline: 2px solid var(--xh-accent); outline-offset: -2px; }` (plus the hover wash/ink so focus and hover read identically). The ring is now drawn *inboard*, so `overflow-y: auto` on `.table-scroll` cannot cut it. Verified by focusing the first, a middle and the last header in both themes — closed rectangle on all four sides every time (`/tmp/xh-design/r2/c-focus-{first,m,last}-{light,dark}.png`). 2. **Chip specificity (council #2).** `button.filter-chip[data-on="true"]:hover` now restates `border-color: var(--xh-accent)` at matching specificity, so the selected chip keeps its accent border while its fill deepens. Verified on the SkillCoveragePanel `files 18` chip, both themes. 3. **No more guillotined glyphs (council #3).** `mask-attachment` does not exist in any engine (probed: `CSS.supports("-webkit-mask-attachment","local")` → `false`), so the `local`/`scroll` conditional trick cannot be ported to a mask. Instead `.table-scroll` carries a real `mask-image`/`-webkit-mask-image` two-stop ramp driven by two custom properties that are **zero-length until measured**, and `table.tsx` gained `useClippedEdges`  …

**Council:** REJECTED (score 6.5)
- DARK-MODE STATE INVERSION IN THE SEGMENTED CONTROL (index.css `.XhToggleGroup`). Hovering an *unselected* segment makes it brighter than the *selected* one, so the selection is ambiguous under the pointer. Measured in dark: selected thumb = `color-mix(in srgb, var(--xh-line) 75%, var(--xh-code))` = rgb(39,43,56), relative luminance 0.0244; hovered-unselected = `color-mix(in srgb, var(--xh-muted) 12%, transparent)` over `--xh-code` = rgb(46,50,63), luminance 0.0332 — 36% brighter. Both states also take full `--xh-ink`, so the label weight/colour no longer separates them either. Visually confirm …
- DARK-MODE SELECTED-SEGMENT RING IS INVISIBLE (index.css `.XhToggleGroup [data-state="on"]`). The thumb's `box-shadow: 0 0 0 1px var(--xh-line)` draws rgb(42,46,58) against a thumb fill of rgb(39,43,56) — a 1.03:1 boundary, i.e. no ring at all — and the companion `0 1px 2px var(--xh-shadow)` is rgba(0,0,0,.45) on a dark ground, also invisible. So in dark the selection rests on a lightness delta alone, which is exactly what defect 1 then overturns. Fix: give the dark thumb a ring that actually reads against its own fill (e.g. `box-shadow: 0 0 0 1px color-mix(in srgb, var(--xh-muted) 40%, var(--x …
- TABLE SYSTEM BLOWS OUT TO 6.3x THE VIEWPORT IN THE RECORDS DRILL-DOWN. At http://localhost:5173/?session=d1b93fb5-2953-43a7-b52f-5b1711cfa5da&view=detailed (1440px viewport, both themes) the SessionTurnTable's `.table-scroll` measures scrollWidth 6897px against clientWidth 1086px; the `<table>` itself computes to 6897.27px. Every record card's prose renders on one unwrapped line and runs off-screen — the whole drill-down is only readable by horizontal scrolling (see /tmp/xh-design/rev/D-turns-dark.png). Cause: SessionTurnTable.tsx line 167 already sets `style={{ whiteSpace: "normal" }}` on the …
- SWITCH HIT TARGET IS 20px, 8px UNDER THE STATED 28px FLOOR. `src/components/ui/switch.tsx` renders 34x20 (measured on SkillCoveragePanel's `#ShowIgnored`), making it the smallest interactive element on the page; its 20px minor dimension is also under WCAG 2.2 SC 2.5.8's 24px and survives only via the spacing exception. Keep the 34x20 visual (that reading is correct and idiomatic) but expand the actual hit area to at least 28px tall — e.g. wrap the track in a transparent 28px-tall pressable, or add a `::after` overlay with `top: -4px; bottom: -4px; left: 0; right: 0; content: ""; position: abso …
- FILTER CHIP HEIGHT IS EMERGENT, NOT ON THE SCALE (index.css `.filter-chip`). It measures 30px — derived from `padding: 0.3125rem` + `line-height: 1.35 x 13px` + 2px border, not chosen — while the rest of the kit's small controls are 26 (Button xs), 28 (Button sm, ToggleGroupItem) and 32 (Button default). 30px is off the 4px rhythm the button frame explicitly claims to climb, and the chips sit in the same views as the 28px segmented control. Fix: pin `.filter-chip { height: 28px; display: inline-flex; align-items: center; padding-block: 0; }` (keep the 0.625rem inline padding) so the chip joins …

_Notes:_ METHOD: own screenshots at 1440px and 900px, light and dark, deviceScaleFactor up to 4x, plus DOM/CDP measurement of computed styles, matched CSS rules, and WCAG luminance ratios. Keyboard-driven focus (real Tab presses) after discovering that programmatic .focus() does not trigger :focus-visible, and a 900ms settle before reading outline values because Tamagui transitions outline-color/offset over 100ms (an early mid-transition read made rings look like UA defaults; they are not — every control …

#### Round 3
**Designer changes:** All changes landed in `/Users/jpeak/play/pytest-xharness-eval/report-ui/src/index.css` — the stylesheet half of the primitive kit. No `.tsx` primitive needed touching. `bun run format`, `bun run check`, `bun run test` (61/61) and `make ui-smoke` all pass; probe files deleted. **1. Segmented control: dark state polarity fixed.** The three per-state surfaces are now named custom properties on `.XhToggleGroup` (`--xh-toggle-thumb`, `--xh-toggle-ring`, `--xh-toggle-off-hover`), so a theme restates values instead of duplicating rules. In `.dark` the off-hover moved from a muted lift over `--xh-code` to `color-mix(in srgb, var(--xh-bg) 55%, transparent)` — it recesses below the track rather than rising above it, matching the light theme's polarity. Measured painted pixels: selected thumb rgb(39,43,56) L=0.0244 vs hovered-unselected rgb(22,24,34) L=0.0094 — **selected is now 2.6× lighter, was 36% darker.** The off-hover ink also stops at `74% ink / 26% muted` in both themes, so the label ranks all three states (muted → three-quarters → full) instead of hover and selected both taking full ink. **2. Selected-segment ring now reads.** `--xh-toggle-ring: color-mix(in srgb, var(--xh-muted) 65%, var(--xh-line))`. One value clears WCAG 1.4.11 in both themes against its own thumb fill: **3.20:1 dark** (was 1.03:1), **3.27:1 light** (was 1.28:1 — the light thumb had no readable edge either, so …

**Council:** REJECTED (score 7.5)
- `src/components/ui/collapsible.tsx` — the Trigger has NO hover state. Verified by byte-comparing element screenshots of `.env-trigger` before and after `hover()`: identical (`hover changes pixels: false`), on a control measuring 1008 x 40px. Add `hoverStyle` and `pressStyle` to `TamaguiCollapsible.Trigger` inside `CollapsibleTrigger` (props, not stylesheet — Tamagui's `:root:root ._bg-…hover:hover{…!important}` atomics outrank any plain class rule, per the note at index.css:776). Concretely: `hoverStyle={{ backgroundColor: "$code", color: "$color" }}`, `pressStyle={{ backgroundColor: "$code",  …
- `src/components/ui/collapsible.tsx` — the Trigger inherits Tamagui's `_fd-column` / `_ai-center` frame defaults, so the disclosure renders as a full-card-width, centre-aligned two-line stack (chevron on its own line ABOVE the label). Measured computed style: `display:flex, flex-direction:column, align-items:center, text-align:center, width:1008.03px`. This directly contradicts `.env-trigger` in index.css (`display:flex; align-items:center; gap:0.25rem`), which is silently losing. Fix on the Tamagui side in `CollapsibleTrigger`: pass `flexDirection="row"`, `alignItems="center"`, `justifyContent …
- `src/components/ui/collapsible.tsx` — after the two fixes above, confirm the trigger still clears the kit's 28px hit-target floor once it stops spanning the card (it is currently 40px tall only because of the stacked layout). Give it an explicit `minHeight={28}` on `TamaguiCollapsible.Trigger` so the shrink-to-content row layout cannot drop it below the floor that `Button size="sm"`, `.filter-chip` and the toggle-group segments all hold to.

_Notes:_ METHOD: own screenshots at 1440px and 900px, light + dark, at deviceScaleFactor 3, framing each primitive with scrollIntoViewIfNeeded; plus two in-page instrumentation passes (canvas-resolved colour parsing for real WCAG ratios over `color-mix()` fills, computed-style dumps, hit-target measurement including `::after` extensions, pixel-diffing of hover states). Probe files deleted. Gates re-run by me: `bun run format`, `bun run check` (tsc + eslint + prettier) and `bun run test` (61/61) all pass. …

### chart-frames
#### Round 1
**Designer changes:** Probe files are gone; all four in-scope files edited, gate green. **`common.tsx` — ChartPanel** - Heading and note now reuse `CardTitle` / `CardDescription` instead of ad-hoc `15/22` + `13/18` values. A chart heading is now typographically the *same object* as a panel heading (15/20, 600, `-0.1` tracking; note 12.5/18 muted capped at 78ch) — before, charts were the only headings on the page with their own size, line-height and unbounded measure. - Structure is `gap`-driven (3px title→note, 16px header→plot) instead of a stray `marginBottom={8}`, so noted and un-noted charts share one rhythm. **`plotly.ts` — Plotly layout** - `baseLayout` now deep-merges per-axis frames, so the two axes can be treated differently from one place. **Y keeps its gridlines and loses its spine; X keeps a spine plus 4px outward ticks and loses its gridlines.** Every vertical rule that used to cut through the waterfall bars and the accumulation lines is gone — what's left is a baseline and a ladder. - Margins tightened `16/16/44/64` → `12/8/40/56`; `automargin` (already on both axes) grows them back when a tick or title needs room, so the frame no longer reserves a gutter it doesn't use. Buys ~60px of plot width per chart. - Hoverlabel: `namelength: -1` (Plotly truncated series names to 15 chars, so "new context (input + cache write)" and "new context (…)" read as the same series), `align: "left"`, 11p …

**Council:** REJECTED (score 6.5)
- `Plot.tsx` — the legend focus ring is CLIPPED on the first and last chip. `ChartLegend`'s YStack is a scroll container (`overflowY: "auto"`, `overflowX: "hidden"`) and each chip is exactly the container's 240px width, so the `focusVisibleStyle` outline (`outlineWidth: 2`, `outlineOffset: 1`) extends 3px outside the clip box and is cut off. Verified: focusing chip 0 on the overview legend renders a three-sided ring with no top stroke (/tmp/xh-design/cc-ovfocus-first.png). Fix by moving the ring inside the chip — `outlineOffset: -1` — or by adding 4px padding to the scrolling YStack and reducing …
- `plotly.ts` — the hover box has no elevation. `baseLayout.hoverlabel` uses `bgcolor: t.panel`, the exact background of the card the plot sits on, with `bordercolor: t.grid`, the faintest line token (light `#e6e8ee` on `#ffffff` = 1.16:1). Over an empty plot region the tooltip is a white rectangle on white held together by an invisible hairline (see /tmp/xh-design/cc-hover-light.png — it only reads because it happens to overlap a black bar). Change `hoverlabel.bordercolor` to `t.muted` (6.2:1 light, 6.7:1 dark) so the box has a real boundary in both themes; keep `bgcolor: t.panel`. Verify by ho …
- `plotly.ts` — the x-axis "baseline" is indistinguishable from the y gridlines, so the file's own stated goal ("what is left is a baseline and a ladder") is not achieved. `xAxisFrame` uses `linecolor: t.grid` / `tickcolor: t.grid` at `linewidth: 1` while `yAxisFrame` uses `gridcolor: t.grid` at `gridwidth: 1` — same token, same weight (see /tmp/xh-design/pp-TurnTiersChart-light.png: the 0-line is visually identical to the 20k/40k/60k rules). Add an `--xh-axis` token to `src/index.css` (`:root` ≈ `#c8ccd6`, `.dark` ≈ `#3a3f4e`) and to `tamagui.config.ts`, expose it on `ChartTheme` (with a matchi …
- `ChartAxisToggle.tsx` — the three text objects in the row do not share a baseline. `alignItems="center"` on the wrapping XStack optically centres the 19px-tall "Chart x-axis" label, the 32px ToggleGroup and the 36px two-line gloss on a common midpoint (all three measure a centre of 858.6px at a 1440 viewport), which puts the label's baseline ~8px BELOW the gloss's first-line baseline — clearly visible in /tmp/xh-design/pa-toggle-w1440-dark.png, where "Chart x-axis" sits lower than "Per line, a value holds…". Change `alignItems` to `"baseline"`, or wrap the label + ToggleGroup in their own cent …
- `ChartAxisToggle.tsx` — the gloss re-declares `CardDescription` typography by hand (`color="$muted" fontSize={12.5} lineHeight={18}`) at a DIFFERENT measure (`maxWidth: "72ch"` vs CardDescription's `78ch`), so the two muted 12.5px paragraphs stacked in the same session column wrap at two different measures. This is exactly the duplication `ChartPanel` was just refactored to remove. Replace that `<Text>` with `<CardDescription flexShrink={1}>` and delete the inline `maxWidth`. While there, the `fontSize={12.5} lineHeight={18} fontWeight="600"` control label is the only 12.5/600 pairing in the T …

_Notes:_ WHAT THE DESIGNER GOT RIGHT (verified, do not undo): - `ChartPanel` reusing `CardTitle`/`CardDescription` is correct and measurably lands: heading computes 15px/20/600/-0.1px, note 12.5/18 capped at 611px (78ch), matching `CardHeader` exactly. The gap-driven 3/16 rhythm matches `CardHeader`'s own gap 3 and the Card's gap 16 — coherent, not ad hoc. - The off-state redesign is genuinely good. Confirmed in /tmp/xh-design/cc-leg-off-light.png and cc-hover-dark.png: the swatch empties to a 1.5px outl …

#### Round 2
**Designer changes:** Changed, all with light/dark verification: **`src/components/charts/Plot.tsx`** — `ChartLegend`'s scrolling `YStack` now carries `padding={3}`, exactly the reach of the chip's focus ring (2px outline + 1px offset). Chips are 234px inside a 240px column, so the ring is drawn inside the clip box instead of being sheared off. Verified: first and last chip focused, light and dark, all four sides drawn (`/tmp/xh-design/r2c-focus-*.png`), and in a forced-overflow legend scrolled to its end the bottom stroke survives too (`/tmp/xh-design/z-legend-overflow-light.png`) — `box-sizing: border-box` keeps `maxHeight` (the chart height) unchanged. **New `--xh-axis` token** — `src/index.css` (`:root` `#c8ccd6`, `.dark` `#3a3f4e`, with a note that, like `--xh-shadow`, a theme file does not supply it), `src/tamagui.config.ts` (`axis: "var(--xh-axis)"`), plus `ChartTheme.axis`, the `FALLBACK` entry and the getter in `plotly.ts`. **`src/components/charts/plotly.ts`** - The baseline now outranks the ladder: `axis` drives the x spine, its ticks and the y zero line; `grid` is gridlines only. Critically this had to change in the `axis()` helper as well as `xAxisFrame`/`yAxisFrame` — every chart spreads `axis(theme, …)` over the frames, so the frame values alone would have been overwritten with `t.grid`. At 3× the 0-line and ticks now read a clear step darker than the 20k/40k/60k rules (`/tmp/xh-desig …

**Council:** REJECTED (score 6.5)
- src/components/charts/Plot.tsx — the legend chip's hover and press washes contradict the stylesheet's own documented recipe and are the faintest interactive affordance in the product. Measured against the panel: hoverStyle `$code` = 1.12:1 light / 1.11:1 dark; pressStyle `$line` = 1.27:1 / 1.28:1, i.e. press is barely distinguishable from hover. index.css states verbatim above `.xh-table tbody tr:hover` that "Row and header hovers are a wash of the muted ink rather than the flat `code` surface: it composites over whatever the row already sits on ... and lands at the same perceived strength in  …
- src/components/charts/Plot.tsx — a focused chip's ring has zero clearance from its neighbour and lands 2px inside it. The ring reaches 3px (outlineWidth 2 + outlineOffset 1) but the legend's `gap` is 1, measured live (chipInsetTop 3, inter-chip gap 1, chipH 28). Visible in /tmp/xh-design/rc-ring-{light,dark}.png: the ring's bottom stroke sits directly on the next chip's box with no gap, and when one chip is keyboard-focused while another is moused the neighbour's hover wash paints under the ring's stroke. Change the ChartLegend YStack's `gap={1}` to `gap={4}` (on the 4px rhythm) so a focused c …
- src/components/charts/Plot.tsx — the swatch's definition hairline does not define anything, and in dark mode it leaves a colour key below the non-text contrast minimum. The swatch's `boxShadow: inset 0 0 0 1px var(--xh-line)` measures 1.27:1 (light) / 1.28:1 (dark) against the panel — invisible in both themes — so a swatch whose series colour is close to the panel has no edge at all. Live-sampled, the dark-theme `cache read (re-read context)` swatch renders rgb(71,85,105) on panel #171a23 = 2.32:1, under the WCAG 1.4.11 3:1 floor for a graphical object required to understand the chart, with no …
- The new `--xh-axis` token is the only chart colour a project cannot theme, and its stated invariant silently breaks on any rebrand. Verified live: `document.documentElement.style.getPropertyValue('--xh-grid')` returns an inline value written by applyTokens, while `--xh-axis` returns empty and falls through to the index.css literal. Every other Plotly-consumed colour (grid, plot, ink, muted, accent, series, waterfall) is project-overridable; a project that overrides `grid` via `xharness_report_design_tokens` keeps #c8ccd6/#3a3f4e and the documented "a clear step darker than grid" relationship i …

_Notes:_ Verified independently with my own screenshots at 3x DPR in both themes plus a 900px narrow viewport; probe files deleted. Gate: `bun run format`, `bun run check` (tsc + eslint + prettier) and `bun run test` (8 files, 61 tests) all pass on the current tree — not a rejection reason. WHAT IS GOOD AND SHOULD NOT BE TOUCHED. ChartPanel is the strongest thing in the unit: it reuses CardTitle (15/20, 600, -0.1 tracking) and CardDescription (12.5/18, muted, 78ch measure) verbatim and its `gap={3}` is e …

#### Round 3
**Designer changes:** All four council fixes landed and verified live; format/check/test all pass. **src/components/charts/Plot.tsx** 1. Legend chip hover/press now use the stylesheet's documented row recipe — `color-mix(in srgb, var(--xh-muted) 10%/18%, transparent)` instead of flat `$code`/`$line`. Measured live: hover 1.15:1 light / 1.17:1 dark, press 1.30 / 1.36 — press is now a real step past hover in both themes, and the wash composites over whatever the chip sits on, like `.xh-table tbody tr:hover`. 2. `ChartLegend` YStack `gap={1}` → `gap={4}` (4px rhythm). Measured inter-chip gap is now 4px, so a focused chip's 3px outward ring (outlineWidth 2 + outlineOffset 1) clears its neighbour's box by 1px instead of drawing its bottom stroke inside it; a neighbour's hover wash can no longer paint under the ring. `padding={3}` gutter and the ring itself unchanged. 3. Swatch ON-state hairline `var(--xh-line)` → `color-mix(in srgb, var(--xh-muted) 45%, transparent)`. The hairline composites over the series fill, so the worst cases now clear WCAG 1.4.11: dark `cache read` rgb(71,85,105) went from a 2.29:1 edgeless block to a rgb(108,119,137) boundary at 3.84:1 against the panel; light `cache read` 3.73:1. OFF branch (1.5px series-coloured outline) untouched. 4. `axis` is now a project-themeable token, not a hardcoded literal: added to `ThemeTokens` (src/lib/types.ts), to `SCALARS` (src/lib/tokens.ts), an …

**Council:** REJECTED (score 6.5)
- Ragged plot right edge across the four stacked session charts. Measured live at 1440px in BOTH themes (`.ygrid` bounding rect): TokenWaterfallChart right=1131, ContextWindowChart right=1387, OutputPerTurnChart right=1131, TurnTiersChart right=1131 — a 256px jump on the second chart in a single vertical column of four (left edges all agree at 365). The frame causes it: `ChartLegend` returns `null` when `items` is empty (Plot.tsx:106), so a legendless chart collapses the 240px column + 16px `columnGap` its siblings reserve, and ContextWindowChart therefore uses a bare `<Plot>` and runs full-blee …
- The Plotly hover box is not a member of the page's floating-surface family. `baseLayout.hoverlabel` (src/components/charts/plotly.ts:162-173) sets `bgcolor: t.panel` — byte-identical to the card it floats over — with `bordercolor: t.muted` and no elevation. Measured live on the overview chart: light `fill rgb(255,255,255)` / `stroke rgb(91,96,112)` 1px, dark `fill rgb(23,26,35)` / `stroke rgb(154,160,176)` 1px. That stroke is ~7:1 against its own ground, the loudest border anywhere on the page (every other border is `--xh-line`, ~1.2:1), and it is the ONLY thing separating the box from the car …

_Notes:_ GATE: verified myself — `bun run check` clean (tsc + eslint + prettier), `bun run test` 8 files / 61 tests passed. No hardcoded hex in any in-scope component; the hex map in plotly.ts is the documented pre-load FALLBACK table (the token-definition layer), not a component colour. Element ids, `data-el`, `[data-xh-loading]` and `__XH_PENDING__` all intact. THE FOUR CLAIMS — all four verified live, all four accepted: 1. Chip hover/press. Confirmed `color-mix(in srgb, var(--xh-muted) 10%/18%, transp …

### records
#### Round 1
**Designer changes:** **Changed** (`report-ui/src/index.css` record-card sections, `src/components/records/RecordCard.tsx`, `src/components/records/values.tsx`) — gate passed: `bun run format` / `check` / `test` (61 tests, 8 files) all green, 0 console errors in both themes. Fixes to things that were broken - **`.comp-label` pile-up.** Three or four nested `.comp` labels anchored to the same corner, rendering `VVcdetaàpje` mush over the record's first line of prose. Now one name at a time: hovering a `.comp` names it as a legible bordered chip, and `.comp:has(.comp:hover)` steps the ancestors aside so you get the *innermost* renderer, not all of them. `data-el` untouched; `*fallback` names stay visible at rest because "no renderer claimed this" is diagnostic, not trivia. - **`.env-trigger` was not a control.** Tamagui's `is_View` base won `flex-direction: column` / `text-align: center` / full width, so `record envelope` rendered as a chevron centred *above* a centred caption mid-card. Restated as a fit-content row with hover, expanded and focus-visible states at the kit's 26px tier. (This is the `collapsible.tsx` residual in DESIGN-LOG; fixed from the stylesheet side and verified against computed styles + screenshots.) - **Ragged kv grids.** Each `.kvs` sized its key column from its own longest key, so a card stacked two or three grids with three different left edges. One fixed `6.5rem` key column + …

**Council:** REJECTED (score 5)
- GATE IS BROKEN — automatic rejection. `bun run check` exits 1: `prettier --check .` reports `[warn] DESIGN-LOG.md`. (`bun run test` is green: 61 tests / 8 files.) Run `bun run format`, then re-run `bun run format && bun run check && bun run test` and confirm all three exit 0 before claiming the gate.
- The `.kvs` "one alignment axis" fix does not actually land on the cards it was written for. 16 of the 56 RecordCards on `?session=d1b93fb5-2953-43a7-b52f-5b1711cfa5da&view=detailed` render kv grids at TWO left edges — x=333 and x=345, a 12px jog (verified on #L10, #L11, #L14, #L15, and unchanged at a 900px viewport). Cause: a grid nested in a `.block` inherits that block's `border-left: 2px` + `padding-left: 0.625rem`, while the message-level grid (`ClaudeMessage`'s model/stop/message-id + `Usage`) sits flush in `.rec-body`. Normalise the key column to one x — either indent the message-level g …
- `.code-head .ch-lang` fails WCAG AA in both themes. `color-mix(in srgb, var(--xh-muted) 70%, transparent)` over the code-head band resolves to rgb(138,142,153) on rgb(247,248,250) = 3.07:1 (light) and rgb(116,121,136) on rgb(28,31,43) = 3.78:1 (dark), at 11px — AA requires 4.5:1. Set `.code-head .ch-lang { color: var(--xh-muted) }` (6.3:1 light) and distinguish the language token by weight or letter-spacing instead of by fading it.
- Hardcoded hex colours in a component, and they are the values that actually paint. `src/components/records/blocks.tsx` `BLOCK_COLOUR` inlines `#4338ca`, `#0f766e`, `#6d28d9`, `#065f46`; `index.css:803` inlines `#3f6212`. No `--xh-category-*` custom property is defined in `src/index.css` or `assets/report.tokens.json`, so the fallback is what renders — `.block.xml` computes to `rgb(63, 98, 18)`, the literal. This breaks the tokens-only constraint and silently desyncs a block's left rule from the pill above it when a project overrides the palette. Import `CATEGORIES` from `@/lib/records` and der …
- The head band does not exist. `background: color-mix(in srgb, var(--xh-code) 55%, var(--xh-panel))` computes to a 1.06:1 step against the card body in light and 1.07:1 in dark — the "repeating horizontal beat" promised by the comment at index.css:590-594 is carried entirely by the 1px bottom hairline, and in dark the head is visually indistinguishable from the body. Use `var(--xh-code)` undiluted: 1.12:1 in both themes, and it reuses the recessed-surface value `.xh-pre` and `.code-head` already sit on.
- Three different left edges inside one card, and two on the right. Measured on #L11 at 1440px: `.rec-head` insets 8px (pill left 330.8), `.rec-body` insets 10px (`.env-trigger` left 332.8), block content 22px (`.bhead`/`.code-head`/`.xh-pre` left 344.8); on the right the flip button ends 8px from the card edge while `.code-head` ends 10px. Nothing in the head aligns with anything in the body. Change `.rec-head` padding to `0.25rem 0.625rem` so the head and body share one 10px inset on both edges.
- `.rec-head .ctx` is off the chip tier the stylesheet itself declares. index.css:396-411 states a shared 20px tier for the chips that "sit beside one another in record-card heads" and enumerates four; `.ctx` is a fifth chip in exactly that row at `min-height: 18px` (measured: ctx 18px beside pill 20px). Put `.ctx` on the 20px tier — simplest is to add `.rec-head .ctx` to the `.code-chip, .pill, .tag` grouping rule and drop its own `min-height`.
- Three off-scale font sizes inside the card. A census of every element under `.rec` yields 13px, 12px, 11.5px, 11px, 10px — plus 12.8px (`Comp.tsx` `Mono`, inline `fontSize: "0.8rem"`, 76 nodes), 11.2px (`.el`, `0.7rem`, 56 nodes) and 13.12px (`.list`, `0.82rem`, 55 nodes). Snap them to the scale already in use: `Mono` → `0.75rem`, `.el` → `0.6875rem`, `.list` → `0.8125rem`. While there, move the inline `fontSize` off `Mono`/`Muted`/`Notice` in `Comp.tsx` into `index.css` classes — document content styles from the stylesheet.
- The head's `.spacer` orphans the glossary label when the head wraps. At 900px every one of the 56 heads wraps to 59px, and because `<span class="spacer">` (`flex: 1`) consumes line one, the `RecordCard` label lands alone at the left of line two with a ~600px void before the permalink/flip cluster. `.rec-actions { margin-left: auto }` already does that job, so the spacer is redundant: delete the `<span className="spacer" />` from `RecordCard.tsx` and let the auto margin push the cluster. Re-shoot at 900px and confirm the second head line has no orphan with a hole beside it. Keep `<El name="Reco …
- Head hit targets are under the stated 28px minimum: the permalink button measures 30×26 and the flip button 26 tall, and `.env-trigger` is 26. Raise `.rec-head .flip`, `.rec-actions button` and `.env-trigger` to `min-height: 28px` and lift `.rec-head { min-height }` from 34px to 36px so the controls still centre with 4px of air.

_Notes:_ Method: read every file in scope plus `index.css:390-935`; took my own screenshots at 1440px and 900px in both themes (overview, a tool_use card with a code block, a wrapped head) and read them; and measured computed styles, box geometry and WCAG contrast in-page with Playwright rather than trusting the eye. Ran the gate myself. WHAT IS GENUINELY GOOD (and should not be undone): the three claimed bug fixes are real and I verified two of them directly. `.env-trigger` now computes `display: flex / …

#### Round 2
**Designer changes:** **Gate: `bun run format` = 0, `bun run check` = 0, `bun run test` = 0 (61 tests / 8 files).** Verified with explicit exit codes, twice, after the last edit. All ten council fixes, verified live at 1440px and 900px, light and dark: 1. **Gate** — ran `bun run format` (it was `DESIGN-LOG.md` failing `prettier --check`); all three commands now exit 0. 2. **One key-column axis** (`index.css` `.kvs`) — `.rec:has(.block) .kvs { margin-left: .75rem }`, with `.block .kvs` and `.env-trigger + * .kvs` exempted (they are already on the 12px gutter). Every grid in a card now starts at one x. Proven: over all 112 RecordCards, `new Set(...).size` is 1 on every card at 1440 and 900 — and still 1 with the envelope expanded (`[345,345,345,345,345]` on #L11). Cards with no blocks stay flush; there is no gutter to adopt. 3. **`.ch-lang` AA** — now `var(--xh-muted)`, distinguished by form instead of fade: tracked-out uppercase against the title's sentence case (title also to weight 500). Measured 6.27:1 light / 6.64:1 dark, up from 3.07/3.78. 4. **No hex in components** — `BLOCK_COLOUR` is now `BLOCK_CATEGORY` (block type → category name) and paints through a new `categoryColour()` in `values.tsx` that resolves `--xh-category-*` with the `CATEGORIES` catalogue as fallback. `Pill` in `RecordCard.tsx` uses the same function, and the xml block sets its rule inline from `categoryColour("harness_context …

**Council:** REJECTED (score 7)
- `src/index.css` `.code-head`: its horizontal padding is 8px while `.xh-pre` below it is 10px, so a code block's title does not align with its own code. Measured on the `command`/BASH block in L11 at 1440px: `.code-head` box x=344.8, title `command` starts at x=353.8; `.xh-pre` box x=344.8, code `find` starts at x=355.8 — a 2px jog on the left, and the same 2px on the right (`BASH` right edge 1334.2 vs code content right 1333.2). The two are explicitly built as one joined panel (shared border, joined radius), so the label must sit on the content's inset. Change `.code-head { padding: 0.125rem 0 …
- `src/index.css` `.rec-head .flip`: the flip button computes to `font-size: 11.5px` (inherited from Tamagui's `size="xs"` frame), off the card's own 13/12/11/10px scale, on the single most-repeated control in the unit — 56 instances per turn view. Add `font-size: 0.6875rem` to `.rec-head .rec-actions button` (covering both `.flip` and the permalink) so the head's controls sit on the same 11px tier as `.meta`, `.el` and `.ctx` beside them. Verify `getComputedStyle(document.querySelector('.flip')).fontSize === '11px'`.
- `src/index.css` `.rec-head .meta + .meta::before`: the `·` separator is a `::before` inside a `white-space: nowrap` span, so when the head wraps the dot leads the new line. At 900px this is visible on L4: line 2 reads `· 4,320 chars ctx 3.5% RecordCard`, opening with an orphaned separator glyph. Move the separator onto the preceding item (`.rec-head .meta:not(:last-of-type)::after { content: "·"; margin-left: 0.5rem; color: … }` with the flex gap supplying the trailing space) or otherwise guarantee no wrapped line in the head begins with a `·`. Re-check at 900px on a card whose pill is >30 cha …
- `src/index.css` `.rec-head`: at a 900px viewport **all 56** heads wrap to 61px (measured: every `.rec-head` height is 61, none is 37), and the second line carries only the `RecordCard` glossary label on the left and the two controls on the right — a 24px band that is ~80% empty, on every card. Make the head hold one line at that width: let the meta run shrink (wrap the three `.meta` spans plus `.ctx` in a `min-width: 0; overflow: hidden` group), or suppress the `.el` label below a container width of ~640px, or cap the pill with `max-width` + `text-overflow: ellipsis` (keep the full kind in its …
- `src/components/records/values.tsx` `Pre`: `.xh-pre` has `max-height: 60vh; overflow: auto` and 4 of the 16 code blocks on this turn actually overflow, yet every `.xh-pre` reports `tabIndex === -1` — a scrollable region no keyboard user can scroll (WCAG 2.1.1). Add `tabIndex={0}` to the `<pre>` in `Pre` and give it an accessible name (e.g. `role="region"` with `aria-label` from the block's title/lang). The global `:focus-visible` rule already covers `[tabindex]`, so confirm the accent ring appears on the pre in both themes and does not clip against `overflow: auto` (add `outline-offset: -2px`  …
- `src/index.css` `.list td:first-child`: on a card that stacks a `Kvs` above a `Listing` (L5, `agent_listing_delta`), the two grids share a key axis but not a value axis — the `.kvs .val` column starts at x=448.8 and the `.list` description column at x=456.4, a 7.6px jog between two grids sitting directly on top of each other. Add `min-width: 6.5rem; padding-right: 0.75rem` to `.list td:first-child` so a short name column snaps to the same 104px key column and 12px gutter the `.kvs` uses (a long name column, as on L6, still grows past it). Verify on L5 that `.kvs .val` x and `.list td:last-chil …
- `src/components/records/blocks.tsx` `ClaudeMessage` (or `src/index.css`): on an assistant card the `model / stop / message id` grid and the six-row `Usage` grid render as one undifferentiated 9-row run — internal row pitch is 21.8px and the gap between the two groups is only 6px, so the report's most quantitative block reads as unlabelled continuation rows. The kit already solves this: the Codex `token_count` renderer captions its two usage grids with `.sublabel` (`this call` / `cumulative`) and `ClaudeMessage` captions nothing. Either give `<Usage>` a `.sublabel` caption in `ClaudeMessage`, o …
- `src/index.css` `.env-trigger`: the disclosure has hover and (via the global rule) focus-visible, but no `:active`. Every other control in this unit presses — Tamagui's `ButtonFrame` carries `pressStyle: { scale: 0.97 }` and `button.filter-chip:active` uses the same 0.97 snap — so the record envelope's trigger is the one dead-feeling control in the card body. Add `.env-trigger:active { transform: scale(0.97) }` and add `transform 75ms ease` to its `transition` list, matching the button frame's split timing.

_Notes:_ Gate verified independently by me, twice: `bun run format` = 0, `bun run check` = 0, `bun run test` = 0 (61 tests / 8 files). Hard constraints hold — zero hex/rgb/hsl literals in `src/components/records/*.tsx`, `categoryColour()` resolves `--xh-category-*` with the `CATEGORIES` catalogue as fallback, `data-el="RecordCard"`, `id={L<line>}`, the `<El>` labels, `[data-xh-loading]` and `__XH_PENDING__` are all intact. Screenshots taken myself at 1440x900 and 900x1000, light and dark, plus 2x/3x crop …

#### Round 3
**Designer changes:** Cleaned up all probe files; the gate (`bun run format && bun run check && bun run test`) passes. **Changed — records unit (round 3)** `src/index.css` 1. **`.code-head` padding `0.5rem` → `0.625rem`.** The title now sits on the inset of the `.xh-pre` it is joined to. Verified: `.ch-title` x = `<code>` x = 355.8 on the `command`/BASH block in L11, and the right edges match. 2. **`.rec-head` never wraps.** `flex-wrap: nowrap`, `gap: 0.5rem`, `.rec-head > :not(.pill) { flex: none }`, `.rec-actions { flex: none }` — the kind pill is the only elastic item, and it elides via a new inner `.pill-kind` span (`min-width: 0` + `text-overflow`), with the full kind kept in the pill's `title`. Two container-query steps on `.rec` (`container-type: inline-size`) shed in priority order: ≤640px drops `chars` and the `.el` glossary chip; ≤420px drops the clock. **Measured: all 56 heads are 37px at 1440, 1200, 1024, 900, 820, 760 and 700px viewports** — previously all 56 were 61px at 900px. No head has a second line at all now. 3. **Separator moved onto the preceding item** — `.rec-head .meta:has(+ .meta)::after` with `margin-left: 0.5rem`, replacing the `::before`. A `·` can now only ever *end* a run, never open a line. The narrow steps suppress the dangling dot when the item after it is hidden (`.meta.clock::after`, `.meta.line::after`, specificity-matched so they actually win). 4. **`.list td:fi …

